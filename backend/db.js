// backend/db.js
const { Pool } = require('pg');

// ตั้งค่าการเชื่อมต่อ (สำคัญ: อย่าลืมแก้รหัสผ่านนะครับ!)
const pool = new Pool({
  user: 'postgres',              // ชื่อผู้ใช้ (ปกติคือ postgres)
  host: 'localhost',             // เครื่องเราเอง
  database: 'camera_rental_db',  // ชื่อฐานข้อมูลที่เราเพิ่งสร้าง
  password: '1234',              // <<== 🔴 ใส่รหัสผ่านที่คุณตั้งตอนลงโปรแกรมตรงนี้
  port: 5432,                    // พอร์ตมาตรฐาน
});

// ทดสอบการเชื่อมต่อ
pool.connect((err) => {
  if (err) {
    console.error('เชื่อมต่อฐานข้อมูลไม่สำเร็จ:', err.message);
  } else {
    console.log('เชื่อมต่อฐานข้อมูลสำเร็จแล้ว! (Database Connected)');
  }
});

module.exports = {
  query: (text, params) => pool.query(text, params),
};